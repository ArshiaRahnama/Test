-- این ریسورس ("1") فعلاً هیچ منطق سمت سروری نیاز نداره: هیل/آرمور/آب/غذا/
-- میکروفون همه‌شون کاملاً کلاینت‌ساید محاسبه و مستقیم به NUI خودِ همین
-- ریسورس فرستاده میشن. این فایل فقط برای ساختار استاندارد نگه داشته شده.
